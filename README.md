# Julius Portfolio

Personal portfolio and interactive CV built with React, Vite, Tailwind CSS, Framer Motion, Lucide icons, and React Router.

## Deploy

```bash
npm install
npm run build
npm run deploy
```

`npm run deploy` publishes the `dist` folder with `gh-pages`.
